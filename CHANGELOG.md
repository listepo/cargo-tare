<!-- @generated by git-cliff from cliff.toml — DO NOT EDIT. Regenerate: `just changelog`. -->
# Changelog

Generated by git-cliff from commit subjects (`just changelog`; release-plz on a release PR).
Do not edit by hand — change the commit or `cliff.toml` and regenerate.
Unreleased work is listed first; a `v*` tag starts a version section.

## 0.1.0 — 2026-09-23

### Tasks
- T13: lossy `incremental` pass (`f14d206`)
- T10: config file, JSON report and exit codes (`cce1de5`)
- T20: Linux reflink dedupe and btrfs compression (`bb3a004`)
- T22: share by hardlink where the filesystem cannot clone (`779264b`)
- T38.1: owner for a target dir moved out of its checkout (#2) (`4d18e37`)
- T43: release pipeline with cargo-dist, release-plz and a bump workflow (#3) (`dde5759`)

### Other
- Initial import: cargo-tare through T9.1 (`5aa45f5`)
- Add `cargo tare seed` to clone a target into a fresh checkout (`cf76df1`)
- Evict whole target dirs once every profile of them is evicted (`e234e71`)
- Add `cargo tare advise`, which reads the files and changes nothing (`ab86298`)
- Add the lossy `doc` pass, which removes rustdoc output (`86bf7e4`)
- Compress the cargo home behind --cargo-home (`fbaf442`)
- Report units left behind by a previous toolchain (`01da4e7`)
- Add --across-families, so unrelated projects share too (`b9614f7`)
- Move every platform primitive into src/sys (`b9f3edc`)
- Record the repository URL in plan.md (`2c8a92b`)
- Drop machine-specific paths from the docs and the bench scripts (`1f8f54d`)
- Rename to dunnage; Session library, daemon and adapters for more build systems (T25–T42) (#1) (`9e849be`)
